codecov:
  notify:
    after_n_builds: 8 # one for each job
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.25
    patch:
      default:
        target: auto
        threshold: 0.25
