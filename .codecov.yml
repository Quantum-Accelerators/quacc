codecov:
  notify:
    after_n_builds: 11 # one for each job
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.25
    patch:
      default:
        target: auto
        threshold: 2.0
